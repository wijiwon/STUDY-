const BlockClass = require("./index5");

// require 파일을 가져와서 실행시켜 주는 것.
// require 설명.
const require = (path) => {
    // 1. 해당 파일의 (path)를 가져오고
    // 2. 해당 소스코드를 실행시킨다.
    // 3. retrun module.exports
}

// nodejs 역사
// ES6
// nodejs가 자체적으로 모듈의 문법을 만든 것이다.
// 모듈 시스템을 구축한 것이다.
console.log(BlockClass.obj);
console.log(BlockClass.add());
BlockClass.asd();